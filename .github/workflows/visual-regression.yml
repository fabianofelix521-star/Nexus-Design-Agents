name: Visual Regression

on:
  workflow_dispatch:

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Connect Playwright or Chromatic snapshot commands here once examples are promoted to apps."
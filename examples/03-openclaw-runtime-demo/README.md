# OpenClaw Runtime Demo

```bash
pnpm demo:openclaw
```

This command exports the OpenClaw target bundle and writes `out/handshake-result.json`, simulating capability negotiation against a host runtime.
# Hermes Runtime Demo

```bash
pnpm demo:hermes
```

This command exports the Hermes target bundle and writes `out/handshake-result.json`, simulating capability negotiation against a Hermes-compatible host.
# VS Code Webview Demo

```bash
pnpm demo:vscode
```

This command exports a Nexus VS Code bundle and generates `out/webview-preview.html`, a static preview that mirrors the extension's review panel structure outside the editor host.
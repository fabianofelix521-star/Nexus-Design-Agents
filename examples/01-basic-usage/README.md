# Basic Usage

```bash
pnpm install
pnpm build
node packages/cli/dist/index.js export --brief examples/01-basic-usage/brief.json --target vscode --output examples/01-basic-usage/out
```

The output folder will contain the design workflow result, a host-specific instruction bundle, a CSS token file, and a starter framework component.
# Changesets

Use Changesets to version and publish the public Nexus packages.

```bash
pnpm changeset
pnpm version-packages
pnpm build
pnpm release:publish
```

The VS Code extension package remains private and is excluded from publication.